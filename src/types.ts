import { type CSSProperties } from 'react';

export type CommonType = {
  /**
   * @description 外层样式
   * @type CSSProperties
   */
  style?: CSSProperties;
  /**
   * @description 外层class
   */
  className?: string;
};
